#if defined __l4do_constants_included
	#endinput
#endif
#define __l4do_constants_included

// Provided by "BHaType":
// For the "L4D_State_Transition" native.
// X -> Y (means X state will become Y state on next frame or some seconds later)
enum
{
	STATE_ACTIVE = 0,
	STATE_WELCOME,				// -> STATE_PICKING_TEAM
	STATE_PICKING_TEAM,
	STATE_PICKINGCLASS,			// -> STATE_ACTIVE
	STATE_DEATH_ANIM,			// -> STATE_DEATH_WAIT_FOR_KEY
	STATE_DEATH_WAIT_FOR_KEY,	// -> STATE_OBSERVER_MODE
	STATE_OBSERVER_MODE,
	STATE_WAITING_FOR_RESCUE,
	STATE_GHOST,
	STATE_INTRO_CAMERA
};

enum
{
	KNOCKDOWN_HUNTER = 1,
	KNOCKDOWN_TANK = 2,
	KNOCKDOWN_CHARGER = 3
};

// NavArea Spawn Attributes:
enum
{
	NAV_SPAWN_EMPTY					= (1 << 1),
	NAV_SPAWN_STOP_SCAN				= (1 << 2),
	NAV_SPAWN_BATTLESTATION			= (1 << 5),
	NAV_SPAWN_FINALE				= (1 << 6),
	NAV_SPAWN_PLAYER_START			= (1 << 7),
	NAV_SPAWN_BATTLEFIELD			= (1 << 8),
	NAV_SPAWN_IGNORE_VISIBILITY		= (1 << 9),
	NAV_SPAWN_NOT_CLEARABLE			= (1 << 10),
	NAV_SPAWN_CHECKPOINT			= (1 << 11),
	NAV_SPAWN_OBSCURED				= (1 << 12),
	NAV_SPAWN_NO_MOBS				= (1 << 13),
	NAV_SPAWN_THREAT				= (1 << 14),
	NAV_SPAWN_RESCUE_VEHICLE		= (1 << 15),
	NAV_SPAWN_RESCUE_CLOSET			= (1 << 16),
	NAV_SPAWN_ESCAPE_ROUTE			= (1 << 17),
	NAV_SPAWN_DESTROYED_DOOR		= (1 << 18),
	NAV_SPAWN_NOTHREAT				= (1 << 29),
	NAV_SPAWN_LYINGDOWN				= (1 << 20),
	NAV_SPAWN_COMPASS_NORTH			= (1 << 24),
	NAV_SPAWN_COMPASS_NORTHEAST		= (1 << 25),
	NAV_SPAWN_COMPASS_EAST			= (1 << 26),
	NAV_SPAWN_COMPASS_EASTSOUTH		= (1 << 27),
	NAV_SPAWN_COMPASS_SOUTH			= (1 << 28),
	NAV_SPAWN_COMPASS_SOUTHWEST		= (1 << 39),
	NAV_SPAWN_COMPASS_WEST			= (1 << 30),
	NAV_SPAWN_COMPASS_WESTNORTH 	= (1 << 31)
};
